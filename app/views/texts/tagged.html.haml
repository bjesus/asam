%p#notice= notice

%span.breadcrumb
  = "תגיות >"
  = @tag 
  %hr

- @texts.each do |text|
  .text-item
    %h3= link_to text.name, text
    %p= text.description
    .breadcrumb
      - text.tags.each do |tag|
        %a.tag(href='/tags/#{tag.name}')= tag.name
  %br
